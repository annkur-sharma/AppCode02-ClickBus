# Azure Service Bus Configuration
# Replace these with your actual Azure Service Bus connection details

# Primary Connection String from Azure Portal
AZURE_SERVICE_BUS_CONNECTION_STRING=Endpoint=sb://xxx.xxx.xxx.net/;SharedAccessKeyName=xxx;SharedAccessKey=xxx

# Queue Name (will be created automatically if it doesn't exist)
AZURE_SERVICE_BUS_QUEUE_NAME=test-queue

# Optional: Pod/Container identification
POD_NAME=clickbus-backend

# Optional: Log levels
LOG_LEVEL=info

# Note: Copy this file to .env and fill in your actual values
# The connection string should be used as-is (no base64 encoding needed)
